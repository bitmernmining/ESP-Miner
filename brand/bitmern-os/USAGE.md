# Bitmern OS brand marks

Static SVG sources (committed; no remote fetch).

| File | Use |
|------|-----|
| `bitmern-os-lockup.svg` | **Primary** full lockup: B mark + BITMERN + \| + OS (dashboard/wizard header) |
| `bitmern-os-compact.svg` | Tight spaces: B mark + \| + OS |
| `bitmern-os-coin.svg` | Coin-only mark (favicon / loading) |
| `bitmern-os-oled.svg` | OLED splash (one-color white) |

## Colors
- Gold `#F1AE2E` — OS wordmark + coin ring
- White `#FFFFFF` — BITMERN wordmark / OLED mono
- Transparent background

Do not introduce workflows that fetch or auto-commit these assets.
